// Esempio con importazioni di moduli ES6
import './scrollManagement.js';
import './animations.js';

// Codice di inizializzazione generale
document.addEventListener('DOMContentLoaded', () => {
    console.log('Il sito è completamente caricato e pronto.');
    // Qui potresti chiamare funzioni di inizializzazione o setup iniziali, se necessario.
});
